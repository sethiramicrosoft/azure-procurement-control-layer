{
  "name": "azure-procurement-control-layer",
  "version": "1.0.0",
  "private": true,
  "description": "Procurement-aware Azure governance accelerator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:ci": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
